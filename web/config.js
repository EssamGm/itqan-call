/*
 * The one place hosting is named.
 *
 * Changing host — Vercel to Cloudflare, Fly, a VPS, or server/standalone.py on
 * your own machine — means editing API_BASE here and nothing else. Same origin
 * ("/api") works whenever the app and the endpoint are served together.
 */
export const API_BASE = "/api";
